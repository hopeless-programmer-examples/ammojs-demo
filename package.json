{
  "name": "ammojs-demo",
  "version": "1.0.0",
  "scripts": {
    "start": "node dist/server/server.js",
    "build": "tsc -p src/server && tsc -p src/client && browserify dist/client/client.js -o public/bundle.js"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.7",
    "ammojs-typed": "^1.0.6",
    "browserify": "^16.5.2",
    "three": "^0.119.1",
    "typescript": "^3.9.7"
  }
}
